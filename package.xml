<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>cuda_pointcloud_filters</name>
  <version>0.1.0</version>
  <description>
    CUDA point cloud filters that fill the two gaps in Autoware's own CUDA
    preprocessor: a standalone crop box and a random downsample. Both read and
    write cuda_blackboard::CudaPointCloud2, so they chain GPU-resident with
    autoware_cuda_pointcloud_preprocessor's CudaVoxelGridDownsampleFilterNode
    between them. Used by any project whose NDT input chain should stay on the
    GPU; nothing here is specific to one vehicle.
  </description>
  <maintainer email="cshih@csie.ntu.edu.tw">NEWSLab</maintainer>
  <license>Apache-2.0</license>

  <buildtool_depend>ament_cmake_auto</buildtool_depend>

  <depend>cuda_blackboard</depend>
  <depend>rclcpp</depend>
  <depend>rclcpp_components</depend>
  <depend>sensor_msgs</depend>
  <depend>autoware_point_types</depend>

  <test_depend>ament_cmake_gtest</test_depend>
  <test_depend>ament_lint_auto</test_depend>
  <test_depend>autoware_lint_common</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
